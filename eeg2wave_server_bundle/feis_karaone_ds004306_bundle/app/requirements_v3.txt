# v3 reuses the established application dependencies and adds ECAPA for the
# audio-only speaker oracle, librosa for the fixed MFCC inversion conformance
# gate, and official DeepFilterNet for explicitly selected denoising only.
-r requirements.txt
speechbrain==1.0.3
torchaudio==2.8.0
librosa==0.10.2.post1
deepfilternet==0.5.6
